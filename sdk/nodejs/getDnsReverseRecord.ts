// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * The **DNS Reverse Record** can be used to search for and return an existing DNS Reverse Record.
 * If a single match is found, it will be returned. If your search results in multiple matches, an error will be returned.
 * When this happens, please refine your search and make sure that your resources have unique names.
 *
 * > ⚠️  Only tokens are accepted for authorization in the **ionoscloud_dns_reverse_record** data source. Please ensure you are using tokens as other methods will not be valid.
 *
 * ## Example Usage
 *
 * ### By ID
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as ionoscloud from "@ionos-cloud/sdk-pulumi";
 *
 * const example = ionoscloud.getDnsReverseRecord({
 *     id: "record_id",
 * });
 * ```
 *
 * ### By name
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as ionoscloud from "@ionos-cloud/sdk-pulumi";
 *
 * const example = ionoscloud.getDnsReverseRecord({
 *     name: "recordexample",
 * });
 * ```
 *
 * ### By name with partial match
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as ionoscloud from "@ionos-cloud/sdk-pulumi";
 *
 * const example = ionoscloud.getDnsReverseRecord({
 *     name: "record",
 *     partialMatch: true,
 * });
 * ```
 *
 * ### By IP
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as ionoscloud from "@ionos-cloud/sdk-pulumi";
 *
 * const example = ionoscloud.getDnsReverseRecord({
 *     ip: "exampleIP",
 * });
 * ```
 */
export function getDnsReverseRecord(args?: GetDnsReverseRecordArgs, opts?: pulumi.InvokeOptions): Promise<GetDnsReverseRecordResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("ionoscloud:index/getDnsReverseRecord:getDnsReverseRecord", {
        "id": args.id,
        "ip": args.ip,
        "name": args.name,
        "partialMatch": args.partialMatch,
    }, opts);
}

/**
 * A collection of arguments for invoking getDnsReverseRecord.
 */
export interface GetDnsReverseRecordArgs {
    /**
     * [string] The ID of the DNS Reverse Record you want to search for.
     */
    id?: string;
    /**
     * [string] The IP of the DNS Reverse Record you want to search for.
     */
    ip?: string;
    /**
     * [string] The name of the DNS Reverse Record you want to search for.
     */
    name?: string;
    /**
     * [bool] Whether partial matching is allowed or not when using name argument. Default value is false.
     *
     * Either `id`, `ip` or `name` must be provided. If none, or more are provided, the datasource will return an error.
     */
    partialMatch?: boolean;
}

/**
 * A collection of values returned by getDnsReverseRecord.
 */
export interface GetDnsReverseRecordResult {
    /**
     * Description stored along with the reverse DNS record to describe its usage.
     */
    readonly description: string;
    /**
     * The UUID of the DNS Reverse Record.
     */
    readonly id: string;
    /**
     * Specifies for which IP address the reverse record should be created. The IP addresses needs to be owned by the contract.
     */
    readonly ip: string;
    /**
     * The reverse DNS record name.
     */
    readonly name: string;
    readonly partialMatch?: boolean;
}
/**
 * The **DNS Reverse Record** can be used to search for and return an existing DNS Reverse Record.
 * If a single match is found, it will be returned. If your search results in multiple matches, an error will be returned.
 * When this happens, please refine your search and make sure that your resources have unique names.
 *
 * > ⚠️  Only tokens are accepted for authorization in the **ionoscloud_dns_reverse_record** data source. Please ensure you are using tokens as other methods will not be valid.
 *
 * ## Example Usage
 *
 * ### By ID
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as ionoscloud from "@ionos-cloud/sdk-pulumi";
 *
 * const example = ionoscloud.getDnsReverseRecord({
 *     id: "record_id",
 * });
 * ```
 *
 * ### By name
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as ionoscloud from "@ionos-cloud/sdk-pulumi";
 *
 * const example = ionoscloud.getDnsReverseRecord({
 *     name: "recordexample",
 * });
 * ```
 *
 * ### By name with partial match
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as ionoscloud from "@ionos-cloud/sdk-pulumi";
 *
 * const example = ionoscloud.getDnsReverseRecord({
 *     name: "record",
 *     partialMatch: true,
 * });
 * ```
 *
 * ### By IP
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as ionoscloud from "@ionos-cloud/sdk-pulumi";
 *
 * const example = ionoscloud.getDnsReverseRecord({
 *     ip: "exampleIP",
 * });
 * ```
 */
export function getDnsReverseRecordOutput(args?: GetDnsReverseRecordOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetDnsReverseRecordResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("ionoscloud:index/getDnsReverseRecord:getDnsReverseRecord", {
        "id": args.id,
        "ip": args.ip,
        "name": args.name,
        "partialMatch": args.partialMatch,
    }, opts);
}

/**
 * A collection of arguments for invoking getDnsReverseRecord.
 */
export interface GetDnsReverseRecordOutputArgs {
    /**
     * [string] The ID of the DNS Reverse Record you want to search for.
     */
    id?: pulumi.Input<string>;
    /**
     * [string] The IP of the DNS Reverse Record you want to search for.
     */
    ip?: pulumi.Input<string>;
    /**
     * [string] The name of the DNS Reverse Record you want to search for.
     */
    name?: pulumi.Input<string>;
    /**
     * [bool] Whether partial matching is allowed or not when using name argument. Default value is false.
     *
     * Either `id`, `ip` or `name` must be provided. If none, or more are provided, the datasource will return an error.
     */
    partialMatch?: pulumi.Input<boolean>;
}
