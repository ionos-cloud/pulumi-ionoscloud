// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * The [GPU Server data source](https://docs.ionos.com/cloud/compute-services/compute-engine/cloud-gpu-vm) can be used to search for and return existing GPU servers.
 * If a single match is found, it will be returned. If your search results in multiple matches, an error will be returned.
 * When this happens, please refine your search string so that it is specific enough to return only one result.
 *
 * ## Example Usage
 *
 * ### By ID
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as ionoscloud from "@ionos-cloud/sdk-pulumi";
 *
 * const example = ionoscloud.compute.getGPUServer({
 *     datacenterId: "datacenter_id",
 *     id: "server_id",
 * });
 * ```
 *
 * ### By Name
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as ionoscloud from "@ionos-cloud/sdk-pulumi";
 *
 * const example = ionoscloud.compute.getGPUServer({
 *     datacenterId: "datacenter_id",
 *     name: "GPU Server Example",
 * });
 * ```
 */
export function getGPUServer(args: GetGPUServerArgs, opts?: pulumi.InvokeOptions): Promise<GetGPUServerResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("ionoscloud:compute/getGPUServer:getGPUServer", {
        "datacenterId": args.datacenterId,
        "id": args.id,
        "location": args.location,
        "name": args.name,
        "templateUuid": args.templateUuid,
    }, opts);
}

/**
 * A collection of arguments for invoking getGPUServer.
 */
export interface GetGPUServerArgs {
    /**
     * Datacenter's UUID.
     */
    datacenterId: string;
    /**
     * ID of the server you want to search for.
     *
     * `datacenterId` and either `name` or `id` must be provided. If none, or both of `name` and `id` are provided, the datasource will return an error.
     */
    id?: string;
    /**
     * Location of that image/snapshot
     */
    location?: string;
    /**
     * Name of an existing server that you want to search for.
     */
    name?: string;
    /**
     * The UUID of the template for creating a GPU server; the available templates for GPU servers can be found on the templates resource
     */
    templateUuid?: string;
}

/**
 * A collection of values returned by getGPUServer.
 */
export interface GetGPUServerResult {
    /**
     * The availability zone in which the volume should exist
     */
    readonly availabilityZone: string;
    readonly bootCdrom: string;
    readonly bootImage: string;
    readonly bootVolume: string;
    /**
     * list of
     */
    readonly cdroms: outputs.compute.GetGPUServerCdrom[];
    readonly cores: number;
    readonly cpuFamily: string;
    /**
     * The id of the datacenter
     */
    readonly datacenterId: string;
    /**
     * The hostname of the server
     */
    readonly hostname: string;
    /**
     * Id of the attached volume
     */
    readonly id: string;
    /**
     * Location of that image/snapshot
     */
    readonly location?: string;
    /**
     * Name of the attached volume
     */
    readonly name: string;
    readonly nics: outputs.compute.GetGPUServerNic[];
    readonly ram: number;
    readonly securityGroupsIds: string[];
    /**
     * The UUID of the template for creating a GPU server; the available templates for GPU servers can be found on the templates resource
     */
    readonly templateUuid?: string;
    readonly token: string;
    /**
     * Status of the virtual Machine
     */
    readonly vmState: string;
    /**
     * list of
     */
    readonly volumes: outputs.compute.GetGPUServerVolume[];
}
/**
 * The [GPU Server data source](https://docs.ionos.com/cloud/compute-services/compute-engine/cloud-gpu-vm) can be used to search for and return existing GPU servers.
 * If a single match is found, it will be returned. If your search results in multiple matches, an error will be returned.
 * When this happens, please refine your search string so that it is specific enough to return only one result.
 *
 * ## Example Usage
 *
 * ### By ID
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as ionoscloud from "@ionos-cloud/sdk-pulumi";
 *
 * const example = ionoscloud.compute.getGPUServer({
 *     datacenterId: "datacenter_id",
 *     id: "server_id",
 * });
 * ```
 *
 * ### By Name
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as ionoscloud from "@ionos-cloud/sdk-pulumi";
 *
 * const example = ionoscloud.compute.getGPUServer({
 *     datacenterId: "datacenter_id",
 *     name: "GPU Server Example",
 * });
 * ```
 */
export function getGPUServerOutput(args: GetGPUServerOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetGPUServerResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("ionoscloud:compute/getGPUServer:getGPUServer", {
        "datacenterId": args.datacenterId,
        "id": args.id,
        "location": args.location,
        "name": args.name,
        "templateUuid": args.templateUuid,
    }, opts);
}

/**
 * A collection of arguments for invoking getGPUServer.
 */
export interface GetGPUServerOutputArgs {
    /**
     * Datacenter's UUID.
     */
    datacenterId: pulumi.Input<string>;
    /**
     * ID of the server you want to search for.
     *
     * `datacenterId` and either `name` or `id` must be provided. If none, or both of `name` and `id` are provided, the datasource will return an error.
     */
    id?: pulumi.Input<string | undefined>;
    /**
     * Location of that image/snapshot
     */
    location?: pulumi.Input<string | undefined>;
    /**
     * Name of an existing server that you want to search for.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The UUID of the template for creating a GPU server; the available templates for GPU servers can be found on the templates resource
     */
    templateUuid?: pulumi.Input<string | undefined>;
}
