// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * The **Kafka user credentials** data source can be used to retrieve access credentials for a specific user.
 *
 * > ⚠️  In order to avoid storing sensitive data in the state, the user credentials ephemeral resource can be used.
 *
 * ## Example Usage
 *
 * ### By ID
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as ionoscloud from "@ionos-cloud/sdk-pulumi";
 *
 * const kafkaUserCredentialsDs = ionoscloud.getKafkaUserCredentials({
 *     clusterId: "kafka_cluster_id",
 *     id: "kafka_user_id",
 *     location: "kafka_cluster_location",
 *     timeouts: {
 *         read: "1m",
 *     },
 * });
 * ```
 *
 * ### By name
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as ionoscloud from "@ionos-cloud/sdk-pulumi";
 *
 * const kafkaUserCredentialsDs = ionoscloud.getKafkaUserCredentials({
 *     clusterId: "kafka_cluster_id",
 *     username: "kafka_username",
 *     location: "kafka_cluster_location",
 *     timeouts: {
 *         read: "1m",
 *     },
 * });
 * ```
 *
 * ## Attributes reference
 *
 * The following attributes are returned by the data source:
 *
 * * `id` - the ID of the user;
 * * `username` - the name of the user;
 * * `certificateAuthority` - PEM for the certificate authority;
 * * `privateKey` - PEM for the private key;
 * * `certificate` - PEM for the certificate;
 */
export function getKafkaUserCredentials(args: GetKafkaUserCredentialsArgs, opts?: pulumi.InvokeOptions): Promise<GetKafkaUserCredentialsResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("ionoscloud:index/getKafkaUserCredentials:getKafkaUserCredentials", {
        "clusterId": args.clusterId,
        "id": args.id,
        "location": args.location,
        "timeouts": args.timeouts,
        "username": args.username,
    }, opts);
}

/**
 * A collection of arguments for invoking getKafkaUserCredentials.
 */
export interface GetKafkaUserCredentialsArgs {
    /**
     * [string] the ID of the Kafka cluster;
     */
    clusterId: string;
    /**
     * [string] the ID of the Kafka user, can be retrieved using `ionoscloud.getKafkaUsers` data source;
     */
    id?: string;
    /**
     * [string] the location of the Kafka cluster, can be one of: `de/fra`, `de/fra/2`, `de/txl`, `fr/par`, `es/vit`, `gb/lhr`, `gb/bhx`, `us/las`, `us/mci`, `us/ewr`. If omitted, the default location will be used: `de/fra`;
     */
    location?: string;
    timeouts?: inputs.GetKafkaUserCredentialsTimeouts;
    /**
     * [string] the name of the Kafka user, can be retrieved using `ionoscloud.getKafkaUsers` data source;
     */
    username?: string;
}

/**
 * A collection of values returned by getKafkaUserCredentials.
 */
export interface GetKafkaUserCredentialsResult {
    readonly certificate: string;
    readonly certificateAuthority: string;
    readonly clusterId: string;
    readonly id: string;
    readonly location?: string;
    readonly privateKey: string;
    readonly timeouts?: outputs.GetKafkaUserCredentialsTimeouts;
    readonly username: string;
}
/**
 * The **Kafka user credentials** data source can be used to retrieve access credentials for a specific user.
 *
 * > ⚠️  In order to avoid storing sensitive data in the state, the user credentials ephemeral resource can be used.
 *
 * ## Example Usage
 *
 * ### By ID
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as ionoscloud from "@ionos-cloud/sdk-pulumi";
 *
 * const kafkaUserCredentialsDs = ionoscloud.getKafkaUserCredentials({
 *     clusterId: "kafka_cluster_id",
 *     id: "kafka_user_id",
 *     location: "kafka_cluster_location",
 *     timeouts: {
 *         read: "1m",
 *     },
 * });
 * ```
 *
 * ### By name
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as ionoscloud from "@ionos-cloud/sdk-pulumi";
 *
 * const kafkaUserCredentialsDs = ionoscloud.getKafkaUserCredentials({
 *     clusterId: "kafka_cluster_id",
 *     username: "kafka_username",
 *     location: "kafka_cluster_location",
 *     timeouts: {
 *         read: "1m",
 *     },
 * });
 * ```
 *
 * ## Attributes reference
 *
 * The following attributes are returned by the data source:
 *
 * * `id` - the ID of the user;
 * * `username` - the name of the user;
 * * `certificateAuthority` - PEM for the certificate authority;
 * * `privateKey` - PEM for the private key;
 * * `certificate` - PEM for the certificate;
 */
export function getKafkaUserCredentialsOutput(args: GetKafkaUserCredentialsOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetKafkaUserCredentialsResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("ionoscloud:index/getKafkaUserCredentials:getKafkaUserCredentials", {
        "clusterId": args.clusterId,
        "id": args.id,
        "location": args.location,
        "timeouts": args.timeouts,
        "username": args.username,
    }, opts);
}

/**
 * A collection of arguments for invoking getKafkaUserCredentials.
 */
export interface GetKafkaUserCredentialsOutputArgs {
    /**
     * [string] the ID of the Kafka cluster;
     */
    clusterId: pulumi.Input<string>;
    /**
     * [string] the ID of the Kafka user, can be retrieved using `ionoscloud.getKafkaUsers` data source;
     */
    id?: pulumi.Input<string>;
    /**
     * [string] the location of the Kafka cluster, can be one of: `de/fra`, `de/fra/2`, `de/txl`, `fr/par`, `es/vit`, `gb/lhr`, `gb/bhx`, `us/las`, `us/mci`, `us/ewr`. If omitted, the default location will be used: `de/fra`;
     */
    location?: pulumi.Input<string>;
    timeouts?: pulumi.Input<inputs.GetKafkaUserCredentialsTimeoutsArgs>;
    /**
     * [string] the name of the Kafka user, can be retrieved using `ionoscloud.getKafkaUsers` data source;
     */
    username?: pulumi.Input<string>;
}
